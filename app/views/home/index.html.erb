<h1 class="title is-1 has-text-centered has-text-white">Pagina de inicio</h1>

<div class="section">
  <div class="container">
    <div class="container-pagination">
      <% @products.each do |product| %>
        <div class="card">
          <div class="card-content">
            <%= link_to product.name, product %>
          </div>
        </div>
      <% end %>
      <% if (@products.empty?) %>
        No hay productos aún
      <% end %>
    </div>
  </div>
</div>